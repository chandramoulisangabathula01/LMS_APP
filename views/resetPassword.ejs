<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta name="csrf-token" content="<%= csrfToken %>">
    <link rel="stylesheet" href="./css/styles.css">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css">
    <link rel="icon" type="image/png" sizes="16x16" href="/images/icons8-books-doodle-favicons/Web/icons8-books-doodle-16.png">
    <title><%= title %></title>
</head>

<body class="bg-gray-100">
    <div class="container mx-auto">
        <div class="grid grid-cols-12">
            <div class="col-start-3 col-span-8 mt-10">
                <%- include ('header.ejs') %>
                <h6 class="bg-blue-100 py-4 text-xl mb-1 text-center">Reset your forgotten password</h6>
                <p class="py-2 text-lg border-b-2 mb-2 text-center">Don't have an account? <a href="/signup" class="text-green-600 font-semibold hover:underline">Sign-up here</a></p>
                <p class="py-2 text-lg border-b-2 mb-2 text-center">Don't want to reset your password? <a href="/login" class="text-green-600 font-semibold hover:underline">Login</a></p>
                <form action="/resetPassword" method="post" autocomplete="on" class="bg-white p-6 rounded-lg shadow-lg">
                    <input type="hidden" name="_csrf" value="<%= csrfToken %>">
                    <div class="py-3">
                        <label for="email" class="text-gray-900">Existing Email:</label>
                        <input type="email" name="email" id="email" class="border border-gray-300 text-gray-900 rounded p-2 w-full focus:outline-none focus:border-blue-500">
                    </div>
                    <div class="py-3">
                        <label for="password" class="text-gray-900">New Password:</label>
                        <input type="password" name="password" id="password" class="border border-gray-300 text-gray-900 rounded p-2 w-full focus:outline-none focus:border-blue-500">
                    </div>
                    <div class="py-3">
                        <button type="submit" class="text-white bg-green-600 hover:bg-green-700 font-medium rounded px-5 py-2 focus:outline-none">Set Password</button>
                    </div>
                </form>
                <% if(messages && messages.error && messages.error.length > 0)  { %>
                    <% for(var i=0; i<messages.error.length; i++) { %>
                    <p class="bg-red-300 my-4 list-none rounded border-gray-50 px-4 py-2 text-center"><%= messages.error[i] %></p>
                    <% } %>
                <% } %>
            </div>
        </div>
    </div>
    <%- include ('footer.ejs') %>
</body>

</html>
