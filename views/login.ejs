<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <meta name="csrf-token" content="<%= csrfToken %>">
    <link rel="stylesheet" href="/css/new-styles.css" /> 
    <script src="https://cdn.tailwindcss.com"></script>
    <title><%= title %></title>
</head>

<body>
    <div class="grid grid-cols-12"> 
        <div class="col-start-4 col-span-6">[]
            <%- include("header.ejs") %> 
            <h2 class="text-3xl font-semibold my-4">Welcome back! Login to your account</h2>
            <form action="/logging" method="post">
                <input type="hidden" name="_csrf" value="<%= csrfToken %>">
                <div class="py-3">
                    <label for="email" class="text-gray-800">Email:</label>
                    <input type="email" name="email" id="email" class="border border-gray-700 text-gray-700 rounded p-2 w-full">
                </div>
                <div class="py-3">
                    <label for="password" class="text-gray-800">Password:</label>
                    <input type="password" name="password" id="password" class="border border-gray-700 text-gray-700 rounded p-2 w-full">
                </div>
                <div class="py-3">
                    <button type="submit" class="text-white bg-blue-500 hover:bg-blue-400 font-medium rounded px-5 py-2">Sign in</button> 
                </div>
            </form>
            <% if(messages && messages.error && messages.error.length > 0)  { %>
                <% for(var i=0; i<messages.error.length; i++) { %>
                    <p class="bg-red-300 my-4 list-none rounded border-gray-50 px-4 py-2"><%= messages.error[i] %></p>
                <% } %>
            <% } %>
        </div>
    </div>
</body>

</html>
