<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <meta name="csrf-token" content="<%= csrfToken %>">
    <title><%= title %></title>
    <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
</head>

<%- include("header.ejs") %>
<body >
    <div class="grid grid-cols-6">
        <div class="col-start-3 col-span-2 bg-white p-8 rounded-lg shadow-md">
            <h2 class="text-2xl font-semibold text-gray-800 mb-4">Signup as a new user</h2>
            <form action="/users" method="post" class="space-y-4">
                <input type="hidden" name="_csrf" value="<%= csrfToken %>">
                <div class="flex flex-col">
                    <label for="firstName" class="text-gray-700">First Name:</label>
                    <input type="text" name="firstName" class="border border-gray-300 p-2 rounded focus:outline-none focus:border-blue-500" required>
                </div>
                <div class="flex flex-col">
                    <label for="lastName" class="text-gray-700">Last Name:</label>
                    <input type="text" name="lastName" class="border border-gray-300 p-2 rounded focus:outline-none focus:border-blue-500" required>
                </div>
                <div class="flex flex-col">
                    <label for="email" class="text-gray-700">Email:</label>
                    <input type="email" name="email" class="border border-gray-300 p-2 rounded focus:outline-none focus:border-blue-500" required>
                </div>
                <div class="flex flex-col">
                    <label for="role" class="text-gray-700">Role:</label>
                    <select name="role" id="role" class="border border-gray-300 p-2 rounded focus:outline-none focus:border-blue-500">
                        <option value="null">Please select</option>
                        <option value="teacher">Teacher</option>
                        <option value="student">Student</option>
                    </select>
                </div>
                <div class="flex flex-col">
                    <label for="password" class="text-gray-700">Password:</label>
                    <input type="password" name="password" class="border border-gray-300 p-2 rounded focus:outline-none focus:border-blue-500" required>
                </div>
                <div class="flex justify-center">
                    <button type="submit" class="bg-green-500 hover:bg-green-400 text-white font-medium rounded-full py-2 px-6 focus:outline-none focus:ring-2 focus:ring-green-600">Sign up</button>
                </div>
                <% if(messages && messages.error && messages.error.length > 0)  { %>
                    <% for(var i=0; i<messages.error.length; i++) { %>
                        <p class="bg-red-300 my-4 rounded p-2"><%= messages.error[i] %></p>
                    <% } %>
                <% } %>
            </form>
        </div>
    </div>
    <%- include ('footer.ejs') %>
</body>

</html>
